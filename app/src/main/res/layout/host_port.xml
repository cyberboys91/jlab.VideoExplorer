<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="5dp"
    android:id="@+id/llServerDataHostPort"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <ProgressBar
        android:id="@+id/pbLocalStorage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="invisible"
        android:indeterminate="false"
        style="@style/ProgressBarSpecialDir"
        android:layout_alignBottom="@+id/tvServerDataHost"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/tvServerDataHost"
        android:textSize="@dimen/text_size"
        android:textColor="@color/white"
        android:paddingLeft="2dp"
        android:ellipsize="middle"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/tvServerDataPort"
        android:textColor="@color/white"
        android:ellipsize="middle"
        android:textSize="@dimen/text_size"
        android:paddingRight="3dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"/>

</RelativeLayout>